#pragma once

#include <stdint.h>

#define KB(X) (unsigned int)((X) * 1024)
#define MB(X) (unsigned int)(KB(X) * 1024)
#define GB(X) (unsigned int)(MB(X) * 1024)
#define TB(X) (unsigned int)(GB(X) * 1024)

#define u8 uint8_t
#define u16 uint16_t
#define u32 uint32_t
#define u64 uint64_t




